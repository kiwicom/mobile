language: node_js
node_js: 'node' # latest stable Node.js release

install:
  - yarn install

script:
  - ./app/scripts/test.sh

deploy:
  skip_cleanup: true
  provider: script
  script: app/scripts/deploy.sh
  on:
    branch: master

cache: yarn

notifications:
  email:
    on_success: change
    on_failure: always
